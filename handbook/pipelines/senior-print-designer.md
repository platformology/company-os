# Senior Print Designer pipelines

Three pipelines run through this seat: incoming, leftover, outgoing. None of them is a forecast. The seat is unassigned. Who fills it is not recorded.

## Incoming — not recorded

The seat page records the craft cluster and no owner for it. Nobody is recorded handing this seat work. Do not invent a design executive.

## Leftover — unassigned, no brief, no production stack or print vendor, split unrecorded

Five holes, all already named on the leftover page.

- The seat is unassigned.
- There is no brief.
- There is no production stack or print vendor.
- The junior / senior split is not recorded.
- Who fills this seat is not recorded.

Those child stages are the holes the leftover page already names. Nothing gets added to that list here, and none of them gets filled in to make the seat look busy. Do not design. Do not invent a brief, a vendor, a split, or KPI Current.

## Outgoing — craft cluster, no owner recorded, then QC

Reports to the craft cluster, and no owner is recorded for it. Do not invent a design executive.

Then QC, the same three steps for every seat: COO first QC → PA last-gate → merge. The COO is first QC, not the only one, and does not merge. Anything external stops with Michael. Nothing sends.

Source: seats/senior-print-designer.md, leftovers/senior-print-designer.md, seats/junior-print-designer.md, runtime-wiring.md, company-run.md, locks.md
