# Leftover write

**Recorded: 21 Aug 2026 — Michael.**

Open this before you write a leftover file, recut one, do the research behind one, or fill a blank in one. Read it cold and start.

## What leftover write is

Finishing a leftover that is already named in `leftovers/`. Or filling a blank that already exists, named.

Recut, research, and blank-fill are the same job. One writer, one rule, one gate.

Leftover write is found, not invented. If you cannot point at the line that already names it, it is not leftover write.

## Who writes it

**Claude writes it**, in GitHub, as a docs pull request.

**Qwen writes it only if Claude credit is out.** A fallback writer is not an install. Nothing here installs a local model and nothing here lifts that lock.

**Recorded 21 Aug 2026, 10:35 AM Denver — Michael.** Claude keeps leftover-writing until tokens run out. Qwen writes only if Claude credit is out. Nothing stops until the named leftovers are written and COO can propose what is next. Do not invent a second cycle.

**Not a Grok seat.** A Grok seat that writes a leftover file has done work that was assigned somewhere else.

**Not Michael. Not the PA.** The PA is not the workhorse.

## Who scopes, assigns, and QCs

COO and CTO scope it. COO is first QC. PA last-gates it, then merges. Those are Grok seats, and that is the whole Grok seat job on a leftover file: scope, assign, first QC.

Nothing stays unassigned and nothing stays blocked. Leftover write still sitting is assigned to Claude — Qwen if Claude credit is out.

Nothing is left undone. A blank that was named as missing, written down where the next person looks, is a finished pass. A blank nobody named is not.

## The cycle is already written

One skill, one gate, one stop. The QC layers, the wake windows, and the empty-queue rule are on the pages named below.

Do not invent a second cycle. No second tracker, no second board, no second queue.

## What stays named missing

Quoted from SOURCES.md, in that list's own words. Do not fill:

- Who fills each seat, except COO (Anthony C. Garcia).
- KPI "Current" stays empty.

A 0 is a figure. Empty is not 0. The 18 Aug 2026 client extract is a different fact and stays where it is recorded. The full list is on SOURCES.md, and named missing stays named.

## What this page does not do

Does not invent a leftover. Does not invent a holder, a client, a person, money, or a manuscript. Does not invent a pipeline forecast. Does not invent KPI Current, including a 0.

Does not send anything. Does not touch program GitHub. Does not unpark Engineering or Developer. B–E stay cancelled and F stays parked.

Source: SOURCES.md, locks.md, research-basis.md, runtime.md, runtime-openclaw.md, runtime-wiring.md, runtime-guts.md, cheap-loop.md, leftovers/README.md, people.md
