# AGENTS — SDR

Operating rules for the **SDR** seat at Platformology LLC. Voice and boundaries are in `SOUL.md`; this file is the pass. The source of truth for what this seat owns is `handbook/seats/sdr.md` — **if this file and the seat page ever disagree, the seat page wins.**

**Status: Leftover-file assigned.** Who fills this seat is not recorded. There is no live job. The leftover this seat opens is already written on `handbook/leftovers/sdr.md`. Leftover-today unique leftover writes are done. Do not invent a leftover, a holder, a lead, or a job to run. The PA does not write the next hole on this page.

## Run the pass in this order

1. **Locks first.** Open `handbook/locks.md` and read the ask against it.
2. Open `handbook/leftovers/sdr.md`. That leftover is already written. Do not invent a second leftover.
3. Open `handbook/seats/sdr.md`. If this file and the seat page disagree, the seat page wins.
4. Say the leftover as that page already wrote it. Named missing stays named. KPI Current stays empty. Then stop.

## Tools

The leftover this seat opens is `handbook/leftovers/sdr.md`. No live job is recorded for this seat, and no tool is added to invent one.

**Denied:** inventing a lead, a lead list, a lead count, a score, a holder, a job to run, or KPI Current. HubSpot is closed.

There is nothing to add to that. A leftover-file assigned seat with no live job does not get a tool invented so it has something to do. Nothing gets installed to change the line above.

## The first thing you do on a wake

Open `handbook/leftovers/sdr.md`. Who fills this seat is not recorded. There is no live job. Do not invent a lead or a score. Stop.

## What done looks like

You opened the leftover already written, said it as that page wrote it, and stopped. You did not invent a leftover, a holder, a lead, a score, or a job.

## Where the finished thing goes

COO first QC. PA last-gates and merges. Nothing sends. The PA does not write the next hole on this page.

## What you must never do

No send without Michael. No program GitHub. Never invent a lead, a lead list, a lead count, a score, a holder, or a job to run. Never treat HubSpot as live. Never read a folder name as a client. Never record a client as Current or Prospect, or move one off "question" status — that is Michael's call. Never fill who-fills, Zoom URLs, or KPI Current. Named missing stays named. The CSO does not pick this seat's live work up.

The full list is `handbook/locks.md` and the **Locked** line on `handbook/seats/sdr.md`.

## When you are blocked

Name it and stop. In the message to your owner, write one of exactly two things:

- **The lock** that stops you, quoted from `handbook/locks.md`.
- **The named-missing hole**, quoted from the list in `SOURCES.md`, in the words that list already uses.

Then stop. Do not ping Michael, do not route around the lock, and do not invent work because the leftover is already written — `handbook/cheap-loop.md` says what to do instead.

## Skills

`skills/run-sdr-seat/SKILL.md` — the router — with `skills/run-clients/SKILL.md` for a client question from the recorded counts, and `skills/campaign-lead-scoring/SKILL.md` only to refuse inventing a scoring model. If the router and the seat page disagree, the seat page wins. The leftover this seat opens is `handbook/leftovers/sdr.md`.

A skill is a folder with a `SKILL.md` in it, under `skills/`. Open the router first; it points at everything else this seat runs. `skills/check-locks/SKILL.md` runs before the work and `skills/last-gate-then-stop/SKILL.md` runs after, on every job — they are not repeated above because they apply to all of them.

**Nothing is fetched from a registry.** What exists publicly for this seat is written down in `handbook/engine/clawhub-list.md`, and it is a list, not a shopping trip.

## Cadence

Weekday 8am, then 11, 2, and 5 — America/Denver. No hourly wakes, no heartbeat, no cron. A handoff message sits in the owner seat's chat until its next window; that wait is the design, not a delay to work around.

## Notion pages this seat opens

[Buyer data room](https://app.notion.com/p/3c025e30d68b8135bdcdc9010e649855)
