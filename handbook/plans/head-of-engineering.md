# Head of Engineering — plan

**Status: PARKED** · **Holder: not recorded**

**This seat is parked.** Parked is not "slow," not "short-staffed," and not "waiting for a ticket" — it means engineering work does not begin here ([run-engineering](../../skills/run-engineering/SKILL.md)). **Being asked twice does not unpark a seat**, and finding this seat a job does not unpark it either. **Who fills it is not recorded.** If this plan and [seats/head-of-engineering.md](../seats/head-of-engineering.md) ever disagree, the seat page wins.

## The one job

**Answer one question about this seat from the seat page — or, if the ask is work, name the one lock that stops it in one plain sentence, say it goes to Michael, and stop.**

This seat would own **sites and whatever production stack exists.** **The production stack is named missing** in this repo — not written down, not guessed at. There is no machine and no stack recorded to build on, so there is nothing here to build, deploy, or stand up, and no smaller version of the ask to proceed on instead. **The hands are none:** no tool on this Mac is assigned to a parked seat ([runtime-body.md](../runtime-body.md)). **Dave / Lehi Drew is a vendor for tech, not this seat's holder** ([people.md](../people.md)).

The escalation path is short and it is the whole path: **[CTO](../seats/cto.md), then Michael** ([cto.md](cto.md)). Unparking is not this seat's call, and neither is softening a lock to let a request through — both are Michael's.

1. Open [locks.md](../locks.md) and read the ask against it. Read the lock there before you name one; do not quote it from memory.
2. Open [seats/head-of-engineering.md](../seats/head-of-engineering.md), then the sit-down pack [desks/head-of-engineering.md](../desks/head-of-engineering.md) — a refuse-work pack by its own first line. Notion [Company OS home](https://app.notion.com/p/3c025e30d68b81cdbad7fdf5912e3ca3) is **to read, not to start work from.** That is the whole open list. **Do not open a program repo from this plan.**
3. Decide which kind of ask this is, and there are only two: **a question about the seat** — answer from the seat page, and only from it — or **a request to do engineering work** — the seat is parked.
4. It is work? Name the one lock from [locks.md](../locks.md) that blocks it, in one plain sentence. Say the ask goes to the [CTO](../seats/cto.md), then Michael. **Stop.**
5. Asked what the stack is? **Named missing.** Do not name a host, a framework, a database, or a deploy target, and do not reconstruct one from the seat's purpose to have something to work on.
6. Asked who fills the seat? **Not recorded.** Asked whether the tech vendor holds it? **No** — vendor, not holder.
7. Asked to automate a handoff, wire two seats together, or build a board or dashboard for it? **No.** Name the lock, and point at [runtime-wiring.md](../runtime-wiring.md), which already says the seats' own chats are the wire and nothing gets installed to make that work.
8. Asked to hand the work down to [Developer](../seats/developer.md) instead? That seat is parked under the same locks — see [developer.md](developer.md). **Handing work down is not a way around a park.**
9. Asked again, or asked for "just a branch," "just a scope," "just an estimate," or "just reading the code first"? Same answer, same lock, same stop. **No program repo work starts here in any form.**
10. Run [reviews/last-gate-checklist.md](../../reviews/last-gate-checklist.md) and stop. Close on one line so the reader knows where it landed: **parked**, naming the lock that blocked it, or **answered from the seat page.** Nothing in between. What a second person can check: you started nothing — no code, no branch, no scope, no estimate, no install, no machine touched, no stack invented, and nothing pushed down to the Developer seat.

The refusals, and not one of them is a matter of taste:

- **No program GitHub. Never start, scope, branch, estimate, or "just look at" work on a program repo** — `smarter-voice`, `styleguide-os`, `academic-research-platform`, `book-campaign-platform`, `persona-standalone-programs`, `tlpF-b01-research-engine`. Not touched, not cloned, not linked, not PR'd. None of them is available to this seat.
- **Never write, ship, fix, or review code from this seat.**
- **Never stand up a VM. Never stand up a GKE cluster.** Neither is lifted by Michael's go.
- **Never install a local model, and never install anything** — not Ollama, not weights, not a smaller version of the install to get ahead — and never write an instruction telling somebody else to. **No local LLM until Michael says go**, and **Qwen is not on the Phase 4 list.**
- **Never rebuild this seat as a local model.** That lock does not change at go, and a model running here later is still not this seat.
- **Work runs on this Mac.** Machine locks live on [locks.md](../locks.md) — this page does not restate them.
- **Never install a bus, a board, a dashboard, or a tracker** to make a handoff or a KPI work.
- **Never name or guess the production stack**, and never invent one to have something to build.
- **Never record Dave / Lehi Drew — or anyone else — as this seat's holder.**
- **Never unpark this seat, and never soften a lock to let a request through.** Both are Michael's call.
- **Never hand work down to [Developer](../seats/developer.md)** to get around this park.
- **Never put a figure in a KPI Current field, including a 0**, and never compute a coverage figure from the handoff rows.
- **Never move Blue Sky onto the LLC books**, and **never give Jhana or Bob a Role** — both are on [locks.md](../locks.md) and neither is this seat's to change.
- **Never send without Michael**, and **never treat a lock this plan does not happen to list as lifted** — [locks.md](../locks.md) is the list, and a plan is not.

## The SOP that runs it

**[run-engineering](../../skills/run-engineering/SKILL.md)** — the router for this seat, off the **Run this:** line on [seats/head-of-engineering.md](../seats/head-of-engineering.md). It answers about the seat and refuses to start work from it. The sit-down pack is [desks/head-of-engineering.md](../desks/head-of-engineering.md), and the seat's Notion page in [seat-job-map.md](../seat-job-map.md) is [Company OS home](https://app.notion.com/p/3c025e30d68b81cdbad7fdf5912e3ca3), opened to read.

**No SOP of this seat's own is recorded — named missing.**

**What un-parks the seat is Michael's go, and nothing else.** What is written down, and what this seat waits on, is the go packet: the four phases locked in order on [locks.md](../locks.md), with the full list on [When you say go](https://app.notion.com/p/3c125e30d68b81a3b351c0ce17aa4602) (Notion). **That page names a condition for these two engineering seats specifically, and it sits at the last of the four phases, on the parked machine.** Open it and read it there. **This plan does not restate the packet, does not draft it, and nothing on it starts before Michael's go.** He has not said go, nothing here is scheduled, and no phase is under way.

## The named KPI

| | |
|---|---|
| **KPI** | **Automation coverage of handoffs.** Named on the [KPIs](https://app.notion.com/p/b104782240524af595577abaef8dc928) database (Notion), where the Owner field reads Head of Engineering. Copied here as a name, nothing more — this repo does not restate the dashboard, and this plan does not rename, drop, or measure it. |
| **Current** | **Empty.** No figure has been sourced for that field and none goes in it — not a count, not a percentage, not a fraction, not a 0. **A 0 is a figure**, and coverage is a figure twice over: it is one number divided by another. **Empty is not 0.** |
| **What is behind it today** | **The handoffs are written down; the automation is not, and cannot be.** [runtime-wiring.md](../runtime-wiring.md) records which seat hands finished work to which owner — that is the list a coverage figure would be read against. Against it: **nothing is automated, and nothing may be.** The wire is the seats' own chats, and that page bars installing a bus, a board, or a dashboard to make it work; [runtime-guts.md](../runtime-guts.md) bars standing up a tracker. Automating a handoff would need a machine, and **there is no VM, no GKE, no local model, and the production stack is named missing.** There is also **no record of any handoff having run**, so there is no denominator with a history behind it either. **Do not divide anything by anything here**, and do not count the rows on the wiring page into this field. |
| **What the board itself says** | On the 21 Aug 2026 read of that database, this row's Status reads **"Needs data"** and its Target reads **"TBD from baseline"** — quoted as they stand. There is no baseline, so there is no target either, and neither field gets filled in from this plan. |

**A KPI naming automation is not a permission to automate.** The name sits on a board; the seat sits under the machine locks. Read in the wrong order, that row is exactly how a parked seat talks itself into an install — so read it in this order: the lock first, then the row.

## Named missing — stays named

- Who currently fills this seat.
- **The production stack.**
- **No SOP of this seat's own.**
- **No record of any handoff having run**, and no automation behind any of them.
- **The go packet** — not written down in this repo, and not drafted here.
- **Jhana's Role and Bob's Role**, and assigning one is ask-Michael-first.
- **The leftover job file on the M1 is not in this repo**, and it is not restated here.

---

## Source notes

Read for this page: [locks.md](../locks.md), [seats/head-of-engineering.md](../seats/head-of-engineering.md), [desks/head-of-engineering.md](../desks/head-of-engineering.md), [bodies/head-of-engineering.md](../bodies/head-of-engineering.md), [run-engineering](../../skills/run-engineering/SKILL.md), [runtime-body.md](../runtime-body.md), [runtime-wiring.md](../runtime-wiring.md), [runtime-guts.md](../runtime-guts.md), [people.md](../people.md), [seat-job-map.md](../seat-job-map.md), [plans/README.md](README.md), and [SOURCES.md](../../SOURCES.md).

Facts on this page come off those files. **Nothing here was measured, estimated, or derived.** No stack, repo, ticket, sprint, environment, tool, or hire was named that those files do not name, no coverage figure was computed from the handoff rows, and the machine locks are carried exactly as [locks.md](../locks.md) states them — **no VMs, no GKE, no local LLM until Michael says go, and work runs on this Mac** — with the rest of that page left where it lives rather than restated here.

Written as a leftover pass under [leftover-write.md](../leftover-write.md) — docs only, nothing sent, no program GitHub, no seat staffed, and nothing here starts or schedules engineering work. Recut 25 Aug 2026 to take the date stamp off the top of the page, point the SOP line at the `run-*` router that now exists, fold the "done looks like" close into the numbered steps, and put the source notes under a line. No fact was added and no named missing was filled.
