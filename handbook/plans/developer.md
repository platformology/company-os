# Developer — plan

**Status: PARKED** · **Holder: not recorded**

**This seat is parked**, under the same locks as [Head of Engineering](../seats/head-of-engineering.md) above it ([seats/developer.md](../seats/developer.md)). Its purpose is to write and maintain code **once there is a machine and a stack to write it on.** Neither exists, so no code starts here and there is no smaller version of the ask to proceed on instead. **Who fills it is not recorded.** If this plan and the seat page ever disagree, the seat page wins.

## The one job

**Answer one question about this seat from the seat page — or, if the ask is code, name the one lock that stops it in one plain sentence, say it goes up, and stop.**

There is **no recorded stack and no machine to write code on.** That is the honest state of the seat, and a job description is not a reason to begin. **The production stack is named missing** ([seats/head-of-engineering.md](../seats/head-of-engineering.md)) — do not name a language, a framework, a host, or a repo layout to fill the gap. **The hands are none:** no tool on this Mac is assigned to a parked seat ([runtime-body.md](../runtime-body.md)). **Dave / Lehi Drew is a vendor for tech, not this seat's holder** ([people.md](../people.md)) — if someone says "ask the developer," that is a vendor question for Michael to route, not a seat this repo can hand work to.

**Scope lock:** if you spot a change that looks necessary, that is not permission to make it. **Flag it and hand it up** — [Head of Engineering](../seats/head-of-engineering.md) / [CTO](../seats/cto.md), then Michael ([head-of-engineering.md](head-of-engineering.md), [cto.md](cto.md)). Unparking is not this seat's call.

1. Open [locks.md](../locks.md) and read the ask against it. Read the lock there before you name one; do not quote it from memory.
2. Open [seats/developer.md](../seats/developer.md), then the sit-down pack [desks/developer.md](../desks/developer.md) — a refuse-work pack by its own first line. Notion [Company OS home](https://app.notion.com/p/3c025e30d68b81cdbad7fdf5912e3ca3) is **to read, not to start coding from.** **Do not open a program repo from this plan.**
3. Decide which kind of ask this is, and there are only two: **a question about the seat** — answer from the seat page, and only from it — or **a request to write, fix, ship, or review code** — the seat is parked.
4. It is code? Name the one lock from [locks.md](../locks.md) that blocks it, in one plain sentence. Say the ask goes to [Head of Engineering](../seats/head-of-engineering.md) / [CTO](../seats/cto.md), then Michael. **Stop.**
5. Does the ask assume a stack, a machine, or a repo? Say what is actually missing: **the machine and the production stack (named missing).** **Work runs on this Mac.** Machine locks live on [locks.md](../locks.md) — this page does not restate them. Do not fill either in.
6. Asked who the developer is? **Not recorded.** Asked whether the tech vendor is it? **No** — vendor, not holder, and a vendor question goes to Michael.
7. Asked for a KPI, a velocity, a throughput, a cycle time, or a coverage figure for this seat? **No KPI is named for this seat on the KPI board.** Say that and stop. Do not produce a number and do not adopt one from elsewhere.
8. Spotted something that looks necessary while answering? **Flag it and hand it up.** Noticing is not permission, and "while I'm here" is a second unit — see [runtime-guts.md](../runtime-guts.md).
9. Asked again, or asked for "just a branch," "just a scope," "just an estimate," or "just reading the code first"? Same answer, same lock, same stop. **No program repo work starts here in any form.**
10. Run [reviews/last-gate-checklist.md](../../reviews/last-gate-checklist.md) and stop. Close on one line so the reader knows where it landed: **parked**, naming the lock that blocked it, or **answered from the seat page.** Nothing in between. What a second person can check: you did not start coding — no branch, no commit, no PR, no scope, no estimate, no install, no machine touched, no stack named, no KPI invented — and anything noticed on the way through was flagged upward rather than fixed.

The refusals, and not one of them is a matter of taste:

- **Never write, ship, fix, or review code from this seat.**
- **No program GitHub. Never start, scope, branch, estimate, commit to, or "just look at" a program repo** — `smarter-voice`, `styleguide-os`, `academic-research-platform`, `book-campaign-platform`, `persona-standalone-programs`, `tlpF-b01-research-engine`. **Do not start work on those repos from this page.** None of them is available to this seat.
- **Never invent a KPI for this seat**, and never borrow one from the seat above it or from a public engineering template.
- **Never stand up a VM. Never stand up a GKE cluster.** Neither is lifted by Michael's go.
- **Never install a local model, and never install anything** — not Ollama, not weights, not a partial install to get ahead — and never write an instruction telling somebody else to. **No local LLM until Michael says go**, and **Qwen is not on the Phase 4 list.**
- **Never rebuild this seat as a local model.** That lock does not change at go.
- **Work runs on this Mac.** Machine locks live on [locks.md](../locks.md) — this page does not restate them.
- **Never name or guess the production stack, a language, a framework, a host, or a repo layout.**
- **Never work past what an ask actually declared** — anything outside it gets flagged, not built.
- **Never record Dave / Lehi Drew — or anyone else — as this seat's holder.**
- **Never unpark this seat, and never soften a lock to let a request through.** Both are Michael's call.
- **Never put a figure in a KPI Current field, including a 0** — anywhere on the board, on any seat's row.
- **Never move Blue Sky onto the LLC books**, and **never give Jhana or Bob a Role** — both are on [locks.md](../locks.md) and neither is this seat's to change.
- **Never send without Michael**, and **never treat a lock this plan does not happen to list as lifted** — [locks.md](../locks.md) is the list, and a plan is not.

## The SOP that runs it

**[run-developer](../../skills/run-developer/SKILL.md)** — the router for this seat, off the **Run this:** line on [seats/developer.md](../seats/developer.md). It answers about the seat and refuses to start coding from it. The sit-down pack is [desks/developer.md](../desks/developer.md), and the seat's Notion page in [seat-job-map.md](../seat-job-map.md) is [Company OS home](https://app.notion.com/p/3c025e30d68b81cdbad7fdf5912e3ca3), opened to read.

**No SOP of this seat's own is recorded — named missing.**

**What un-parks the seat is Michael's go, and nothing else.** What this seat waits on is the go packet: the four phases locked in order on [locks.md](../locks.md), with the full list on [When you say go](https://app.notion.com/p/3c125e30d68b81a3b351c0ce17aa4602) (Notion). **That page names a condition for this seat and Head of Engineering specifically, and it sits at the last of the four phases, on the parked machine.** Open it and read it there. **This plan does not restate the packet, does not draft it, and nothing on it starts before Michael's go.** He has not said go, nothing here is scheduled, and no phase is under way.

## The named KPI

**No KPI is named for this seat on the KPI board.**

That is the answer, in those words, and it does not get filled in. On the 21 Aug 2026 read of the [KPIs](https://app.notion.com/p/b104782240524af595577abaef8dc928) database (Notion), no row carries Developer in its Owner field — this is the one seat of the twenty-three with no KPI named for it ([plans/README.md](README.md)). **Do not invent one, do not borrow the one above it, and do not adapt a KPI off a public engineering template.** Naming a KPI for a seat is not this plan's call.

So there is no Current field on this plan to leave empty, because there is no row. **That is not an invitation to open one.** And every Current field elsewhere on that board stays empty — **a 0 is a figure, and empty is not 0** — including on the row belonging to the seat above this one, see [head-of-engineering.md](head-of-engineering.md).

| | |
|---|---|
| **What this seat is pointed at instead** | The live leftover on its own seat page: **the production stack is named missing**, and **who fills this seat is not recorded** ([seats/developer.md](../seats/developer.md)). Both stay named. Neither is work this seat starts, because the seat is parked — naming a hole is the finished answer, not the first step toward filling it. |

## Named missing — stays named

- Who currently fills this seat.
- **The production stack.**
- **No SOP of this seat's own.**
- **No KPI is named for this seat on the KPI board** — and none gets written for it here.
- **The go packet** — not written down in this repo, and not drafted here.
- **Jhana's Role and Bob's Role**, and assigning one is ask-Michael-first.
- **The leftover job file on the M1 is not in this repo**, and it is not restated here.

---

## Source notes

Read for this page: [locks.md](../locks.md), [seats/developer.md](../seats/developer.md), [desks/developer.md](../desks/developer.md), [bodies/developer.md](../bodies/developer.md), [run-developer](../../skills/run-developer/SKILL.md), [seats/head-of-engineering.md](../seats/head-of-engineering.md), [runtime-body.md](../runtime-body.md), [runtime-guts.md](../runtime-guts.md), [people.md](../people.md), [seat-job-map.md](../seat-job-map.md), [plans/README.md](README.md), and [SOURCES.md](../../SOURCES.md).

Facts on this page come off those files. **Nothing here was measured, estimated, or derived.** No stack, language, repo, ticket, sprint, environment, tool, or hire was named that those files do not name, no KPI was invented or borrowed for this seat, and the machine locks are carried exactly as [locks.md](../locks.md) states them — **no VMs, no GKE, no local LLM until Michael says go, and work runs on this Mac** — with the rest of that page left where it lives rather than restated here.

Written as a leftover pass under [leftover-write.md](../leftover-write.md) — docs only, nothing sent, no program GitHub, no seat staffed, and nothing here starts or schedules code. Recut 25 Aug 2026 to take the date stamp off the top of the page, point the SOP line at the `run-*` router that now exists, fold the "done looks like" close into the numbered steps, and put the source notes under a line. No fact was added and no named missing was filled.
